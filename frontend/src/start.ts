// Legacy TanStack Start SSR entrypoint.
// This project has been simplified to a plain Vite SPA, so the old SSR
// server integration is no longer used.

// The file remains only as a placeholder to avoid accidental runtime imports.
